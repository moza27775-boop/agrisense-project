name: agrisense_app
description: "AgriSense Smart Farming App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  intl: ^0.20.2
  firebase_core: ^2.32.0
  firebase_database: ^10.5.7
  flutter_local_notifications: ^17.1.2
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true  # هذا السطر هو الأهم لظهور الأيقونات بدل الرموز!
  